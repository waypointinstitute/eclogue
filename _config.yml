url: https://waypointinstitute.github.io
baseurl: /eclogue

plugins:
  - jekyll-feed
  - jekyll-sitemap

collections:
  team:
    output: false        # we list them on /about/, we don't need individual pages
  books:
    output: true         # optional if you have a /books page and detail pages
    permalink: /books/:name/
